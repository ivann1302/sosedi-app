# Android emulator smoke — 09.09.2026

Среда: sosedi_api35, API 35 arm64, обычный debug APK с текущими UX-правками,
API_BASE_URL=http://127.0.0.1:3000/api/v1 и adb reverse tcp:3000 tcp:3000.
Backend development/console OTP, синтетический аккаунт арендатора.

- Вставка 89990001002 отображается как +7 (999) 000-10-02; ответ OTP
  возвращает +79990001002. Вход с локальным кодом 123456 прошёл.
- Две фотографии выбраны через native picker двумя отдельными добавлениями.
  Обе остались в форме, первое фото осталось главным.
- Форма из трёх шагов пройдена: категория, состояние, описание, цена,
  синтетические район/адрес, точка на карте и обязательные подтверждения.
- Яндекс-карта отобразила реальные тайлы и маркер; точка сохранилась.
- Отправка создала объявление PENDING. Тестовая запись EmulatorCameraTest
  затем скрыта; реальная модерация не выполнялась.
- Фото не загрузились: локальный backend/.env указывает placeholder
  S3_ENDPOINT=https://s3.example.ru, наблюдаются TLS timeouts cleanup.
  В БД у тестового объявления 0 фото. UI показал отдельную ошибку и retry.
  Это открытый blocker хранилища, не успешная проверка end-to-end upload.
- Бронирование на 10.09.2026: даты свободны, 700 ₽ × 1 день, залога нет,
  оплата при передаче, итог 700 ₽. Строк валюты и выплаты владельцу нет.
  Отправка брони закрыта существующим legal/public gate; обход не выполнялся.
- Красный цвет ошибки отправки и сохранение причины проверены widget-тестом;
  исходный отказ пользователя при создании записи на эмуляторе не повторился.

До emulator smoke: analyzer и 325 mobile tests прошли, APK собран.
Новый незапланированный дефект: после посещения Сдать гостем список сохранил
ошибку «Требуется авторизация» после входа; кнопка Добавить открыла рабочую форму.
Требуется отдельная проверка инвалидирования списка при смене auth-сессии.

## Продолжение: локальное хранилище и авторизация

Позднее 09.09.2026 блокировка локальной загрузки устранена: настроен отдельный
MinIO, две фотографии успешно прошли presigned upload и обработку worker.
Объявление одобрено через operator UI с MFA и доступно в Android-каталоге;
галерея переключается 1/2 → 2/2. Ошибка списка после гостевого посещения и входа
исправлена и проверена на эмуляторе. Также исправлен отсутствующий installation
header при подтверждении OTP в админке. `make check` прошёл (326 Flutter-тестов).
Инструкции и границы проверки: [local-storage.md](local-storage.md).
